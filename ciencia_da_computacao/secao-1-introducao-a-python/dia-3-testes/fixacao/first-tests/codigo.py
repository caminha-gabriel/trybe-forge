def is_odd(number):
    'Returns True if number is odd, otherwise returns False'
    return number % 2 != 0


def divide(a_number, b_number):
    'Returns the division of two numerical values'
    return a_number / b_number
